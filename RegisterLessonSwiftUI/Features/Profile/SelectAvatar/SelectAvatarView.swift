
import SwiftUI

struct SelectAvatarView: View {
  @Environment(\.dismiss) var dismiss
  @StateObject var VM = ProfileViewViewModel()
    var body: some View {
      NavigationStack {
        
      }
    }
}

#Preview {
    SelectAvatarView()
}
